name: combustivel_app
description: Controle de Gastos com Combustível
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.6.0
  path: ^1.9.0
  intl: ^0.19.0
  http: ^1.6.0
  provider: ^6.1.2
  pdf: ^3.11.1
  open_file: ^3.3.2
  share_plus: ^10.1.0
  path_provider: ^2.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
